[project]
name = "lessiondesign"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.0",
    "numpy>=1.24",
    "ultralytics>=8.0",
]

[project.scripts]
scanner = "src.cli:main"
